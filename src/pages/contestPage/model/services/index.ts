import {
    fetchMediaWorks,
    fetchNextMediaWorks,
    fetchPopularMediaWorks,
} from './fetchMediaWorks'
import {
    fetchNextTextWorks,
    fetchPopularTextWorks,
    fetchTextWorks,
} from './fetchTextWorks'

export {
    fetchMediaWorks,
    fetchNextMediaWorks,
    fetchNextTextWorks,
    fetchPopularMediaWorks,
    fetchPopularTextWorks,
    fetchTextWorks,
}
